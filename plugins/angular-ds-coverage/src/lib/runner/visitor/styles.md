Angular uses `CssParser` to parse CSS files. The parser is created using the following code:
import { CssParser } from '@angular/compiler';

```ts
import { CssParser } from '@angular/compiler';


const parser = new CssParser();
``` 
